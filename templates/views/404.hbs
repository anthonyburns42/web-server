<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" type="text/css" href="/css/style.css">
    </head>

    <body>
        {{>header}}
        <p>{{errorMsg}}</p>

        {{>footer}}
    </body>
</html>